# Python dependencies are managed with uv — see pyproject.toml and uv.lock.
#
#   cd automation
#   uv sync                  # API service + worker (default deps)
#   uv sync --extra desktop  # + PyAutoGUI for local desktop scripts
#
# Run API locally:
#   uv run uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
#   uv run python -m api.worker
